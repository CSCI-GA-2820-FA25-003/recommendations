apiVersion: v1
kind: Service
metadata:
  name: recommendations
  namespace: app
spec:
  selector:
    app: recommendations
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
